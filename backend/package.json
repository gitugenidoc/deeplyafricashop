{
  "name": "deeply-africa-shop-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Shopify webhook and reveal API for Deeply Africa Mystery Country Pass.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check controllers/webhookController.js && node --check controllers/revealController.js"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.1",
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^4.21.2"
  }
}
